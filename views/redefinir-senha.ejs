
<div class="min-vh-100 d-flex flex-column">
    <div class="container flex-grow-1 d-flex align-items-center justify-content-center py-4">
        <div class="row w-100 justify-content-center">
            <div class="col-12 col-sm-10 col-md-8 col-lg-6 col-xl-5">
                <div class="card shadow-lg border-0">
                    <div class="card-header bg-navy text-white text-center py-4">
                        <div class="mb-2">
                            <i class="fas fa-key" style="font-size: 2.5rem;"></i>
                        </div>
                        <h4 class="mb-0 fw-bold">Redefinir Senha</h4>
                        <p class="mb-0 opacity-75 small">Recupere o acesso à sua conta</p>
                    </div>

                    <div class="card-body p-4">
                        <% if (typeof error !== 'undefined' && error) { %>
                            <div class="alert alert-danger border-0 rounded-3 mb-4">
                                <div class="d-flex align-items-center">
                                    <i class="fas fa-exclamation-triangle me-3 flex-shrink-0"></i>
                                    <div><%= error %></div>
                                </div>
                            </div>
                        <% } %>

                        <% if (typeof success !== 'undefined' && success) { %>
                            <div class="alert alert-success border-0 rounded-3 mb-4">
                                <div class="d-flex align-items-center">
                                    <i class="fas fa-check-circle me-3 flex-shrink-0"></i>
                                    <div><%= success %></div>
                                </div>
                            </div>
                        <% } %>

                        <div class="text-center mb-4">
                            <p class="text-muted mb-0">Digite seu email para receber um link de redefinição de senha</p>
                        </div>

                        <form method="POST" action="/redefinir-senha" class="needs-validation" novalidate>
                            <div class="mb-4">
                                <label for="email" class="form-label fw-semibold">
                                    <i class="fas fa-envelope me-2 text-primary"></i>
                                    Email
                                </label>
                                <input type="email" class="form-control form-control-lg" 
                                       id="email" name="email" required
                                       placeholder="Digite seu email cadastrado">
                                <div class="form-text">
                                    <i class="fas fa-info-circle me-1"></i>
                                    Enviaremos um link para este email
                                </div>
                            </div>

                            <div class="d-grid gap-3">
                                <button type="submit" class="btn btn-primary btn-lg py-3">
                                    <i class="fas fa-paper-plane me-2"></i>
                                    Enviar Link de Redefinição
                                </button>

                                <a href="/login" class="btn btn-outline-secondary btn-lg py-3">
                                    <i class="fas fa-arrow-left me-2"></i>
                                    Voltar ao Login
                                </a>
                            </div>
                        </form>

                        <hr class="my-4">

                        <div class="text-center">
                            <small class="text-muted">
                                Já tem um código de verificação?
                            </small>
                            <br>
                            <a href="/redefinir-senha/verificar" class="btn btn-link btn-sm p-0 mt-1">
                                <i class="fas fa-keyboard me-1"></i>
                                Verificar código manualmente
                            </a>
                        </div>
                    </div>
                </div>

                <!-- Informações de Segurança -->
                <div class="card mt-3 border-0 bg-light">
                    <div class="card-body p-3">
                        <h6 class="card-title text-primary mb-2">
                            <i class="fas fa-shield-alt me-2"></i>
                            Como funciona?
                        </h6>
                        <ul class="list-unstyled small text-muted mb-0">
                            <li><i class="fas fa-check-circle me-2 text-success"></i>Você receberá um email com um link seguro</li>
                            <li><i class="fas fa-check-circle me-2 text-success"></i>O link expira em 15 minutos</li>
                            <li><i class="fas fa-check-circle me-2 text-success"></i>Clique no link para definir nova senha</li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
// Validação do formulário
document.addEventListener('DOMContentLoaded', function() {
    const form = document.querySelector('.needs-validation');
    const emailField = document.getElementById('email');

    emailField.addEventListener('input', function() {
        if (this.checkValidity()) {
            this.classList.remove('is-invalid');
            this.classList.add('is-valid');
        } else {
            this.classList.remove('is-valid');
            this.classList.add('is-invalid');
        }
    });

    form.addEventListener('submit', function(event) {
        if (!form.checkValidity()) {
            event.preventDefault();
            event.stopPropagation();
        }
        form.classList.add('was-validated');
    });
});
</script>
